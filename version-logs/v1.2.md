# Version 1.2 - Changelog

## ✨ Tính năng mới

- **Auto-Export**: Tự động export CSV sau khi scrape xong, tự động chia file nếu >200 items
- **Data Validation**: Validate data trước khi export, cảnh báo dataset lớn (>1000 items)
- **Storage Management**: Auto-cleanup data >24h, check quota trước khi save

## 🐛 Bug Fixes

### 1. State không Reset sau khi Clear + Routing
**Vấn đề**: Sau khi xóa kết quả và routing sang category khác, phải F5 mới scrape được "1 click", phải click lần 2 mới quét detail.

**Fix**: Clear storage state (`scrapeDetailsState`, `paginationState`) trước khi bắt đầu scrape mới, validate và cleanup state trong `handleScrapeListAndDetails`, đảm bảo requestId unique mỗi lần scrape.

### 2. Skip Logic không hoạt động đúng
**Vấn đề**: Khi skip=100, limit=100 → nên scrape items 101-200 nhưng lại scrape 1-100.

**Fix**: Không slice products sớm trong pagination/scroll handlers, trả về tất cả products scraped để caller xử lý skip logic. Apply skip sau khi extract links: `.slice(skipProducts, skipProducts + maxDetails)`, validate đảm bảo có đủ items, scrape đủ số lượng: `totalToScrape = skipProducts + maxProducts`.

### 3. Browser Crash khi Export Large Datasets
**Vấn đề**: Crash khi export >200 items CSV.

**Fix**: Chia thành nhiều files (100 items/file), batch processing, truncation limits (string: 50k, row: 1M, array: 500), depth limit (5).

### 4. Files không Download được
**Vấn đề**: Logs hiển thị nhưng files không xuất hiện.

**Fix**: Tăng delay revoke URL (3000ms), delay giữa downloads (2500ms), double requestAnimationFrame.

### 5. Crash khi xử lý Item thứ 50
**Vấn đề**: Crash ở item 50/100 trong chunk đầu tiên.

**Fix**: Cải thiện error handling, giới hạn keys/object (1000), skip items lỗi thay vì crash.

### 6. Storage Overflow & Data Loss
**Fix**: Check quota trước khi save, auto-cleanup, save vào storage TRƯỚC khi send message.

## 🔧 Cải thiện Performance

- **Memory**: Chia files nhỏ, batch processing, truncation limits
- **Export**: Collect headers từ 5 items đầu, batch join rows, direct download cho files >2MB
- **Code**: Refactor từ 982 → 739 dòng, loại bỏ code trùng lặp

## 📋 Workflow

**Trước**: User scrape → Save → User manually export → Download  
**Bây giờ**: User scrape → Save → Auto-export (nếu enabled) → Auto-split → Download

## ⚠️ Cần Monitor

- Memory issues với datasets >500 items (đã có split files)
- Download failures với >10 files (đã có delays)
- Circular references trong data (đã có WeakSet detection)

## 📝 Files Changed

- `handlers/export-handler.js` - Complete refactor
- `popup-main.js` - Auto-export logic
- `ui/popup-state.js` - Storage management
- `popup.html` - Auto-export checkbox
